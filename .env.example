# Ollama Configuration
# Base URL for the Ollama API endpoint
# For remote Ollama service, use the full HTTPS URL
# For local Ollama (from Docker), use http://host.docker.internal:11434
OLLAMA_BASE_URL=https://your-ollama-endpoint.com

# Authentication token for Ollama API (if required)
# Leave empty if using local Ollama without authentication
OLLAMA_AUTH_TOKEN=your-auth-token-here

# Ollama request timeout in seconds (default: 120, max: 600)
# Increase for large batches or slow connections
# OLLAMA_TIMEOUT=120

# Ollama batch size - number of texts to embed per request (default: 50, max: 500)
# Larger batches are faster but use more memory
# Reduce if you encounter memory issues or 413 errors from your Ollama instance
# OLLAMA_BATCH_SIZE=50

# Base URL of the Store API
# For Docker: http://legal-mcp-store-api:8000
# For local development: http://localhost:8000
LEGAL_API_BASE_URL=http://legal-mcp-store-api:8000

# PostgreSQL Configuration
# Host for the PostgreSQL database
# Use 'postgres' when running in Docker (references the Docker service name)
# Use 'localhost' when running locally outside Docker
POSTGRES_HOST=postgres

# PostgreSQL Credentials
# User is hard-coded to 'postgres' (default PostgreSQL superuser)
# IMPORTANT: Change the password for production deployments!
POSTGRES_PASSWORD=postgres_password
POSTGRES_DB=legal_mcp_db
